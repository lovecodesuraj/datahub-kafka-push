name=datahub-sink-connector
connector.class=com.linkedin.datahub.kafka.DataHubSinkConnector
tasks.max=1
topics=my_topic

datahub.rest.server=http://localhost:8080
datahub.sink.batch.size=100
datahub.sink.report.metrics=false

# Optional: Schema registry URL
# value.converter=org.apache.kafka.connect.json.JsonConverter
# value.converter.schemas.enable=false


steps
Deploy the Connector: Deploy the connector using the Kafka Connect REST API.
Step 5: Start Kafka Connect
Ensure Kafka Connect is running and has access to the DataHub Kafka sink connector JAR files.

